#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../icomoon-build/bin/icomoon-build" "$@"
  ret=$?
else 
  node  "$basedir/../icomoon-build/bin/icomoon-build" "$@"
  ret=$?
fi
exit $ret
